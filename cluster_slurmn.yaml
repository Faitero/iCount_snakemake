# cluster_slurm.yaml - Slurm cluster configuration for icount Snakefile.
__default__:
    time: "16:00:00"
    n: "1"
    c: "1"
    memory: "30G"
    name: "{rule}"
    output: "logs/cluster/{rule}_{wildcards}.out"
    error: "logs/cluster/{rule}_{wildcards}.err"